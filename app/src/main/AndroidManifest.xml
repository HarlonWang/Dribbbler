<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wang.dribbble">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name=".DRApplication"
        android:theme="@style/AppTheme">
        <activity android:name=".module.login.SplashActivity" android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".module.login.LoginActivity" android:label="SIGN IN"/>
        <activity android:name=".module.setting.SettingActivity" android:label="Setting"/>
        <activity android:name=".module.shots.detail.ShotsDetailActivity" android:label="Detail"/>
        <activity android:name=".module.main.MainActivity" android:label="Shots" android:theme="@style/CustomTheme"/>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
</manifest>